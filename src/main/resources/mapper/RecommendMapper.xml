<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.oneplane.recommend.repository.RecommendRepository">

    <!-- 동의 저장 -->
    <insert id="insertAgreement" parameterType="int">
        INSERT INTO recommend (user_id, agreement, created_at)
        VALUES (#{userId}, 'Y', SYSDATE)
    </insert>

    <!-- 최신 recommend_id 가져오기 -->
    <select id="getLatestRecommendId" parameterType="int" resultType="int">
        SELECT recommend_id
        FROM recommend
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
            FETCH FIRST 1 ROWS ONLY
    </select>

    <!-- 최근 agreement 여부 확인 -->
    <select id="getLatestAgreement" parameterType="int" resultType="string">
        SELECT agreement
        FROM recommend
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
            FETCH FIRST 1 ROWS ONLY
    </select>

    <!-- 여행 목적 + 동행자 업데이트 -->
    <update id="updateInput" parameterType="com.oneplane.recommend.dto.RecommendDTO">
        UPDATE recommend
        SET travel_purpose = #{travelPurpose},
            companion = #{companion}
        WHERE recommend_id = #{recommendId}
    </update>

    <!-- 국가 선택 INSERT -->
    <insert id="updateSelection" parameterType="com.oneplane.recommend.dto.RecommendDTO">
        INSERT INTO recommend (
            user_id, country_id, travel_purpose, companion, created_at
        ) VALUES (
                     #{userId}, #{countryId}, #{travelPurpose}, #{companion}, SYSDATE
                 )
    </insert>

    <!-- 피드백 INSERT -->
    <insert id="updateFeedback" parameterType="com.oneplane.recommend.dto.RecommendDTO">
        INSERT INTO recommend (
            user_id, recommend_rating, rating_content, created_at
        ) VALUES (
                     #{userId}, #{recommendRating}, #{ratingContent}, SYSDATE
                 )
    </insert>

</mapper>