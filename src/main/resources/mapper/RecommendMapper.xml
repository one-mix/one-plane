<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.oneplane.recommend.repository.RecommendRepository">

    <insert id="save" parameterType="com.oneplane.recommend.domain.Recommend">
        INSERT INTO recommend (
            user_id, travel_history_id, country_id,
            travel_purpose, companion, recommend_rating, rating_content, agreement
        ) VALUES (
                     #{userId}, #{travelHistoryId}, #{countryId},
                     #{travelPurpose}, #{companion}, #{recommendRating}, #{ratingContent}, #{agreement}
                 )
    </insert>

    <select id="findByUserId" parameterType="long" resultType="com.oneplane.recommend.domain.Recommend">
        SELECT *
        FROM recommend
        WHERE user_id = #{userId}
          AND deleted_at IS NULL
        ORDER BY created_at DESC
    </select>

</mapper>
